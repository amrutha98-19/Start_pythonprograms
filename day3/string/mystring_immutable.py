# Strings are immutable in Python, meaning their values cannot be changed after creation.
mystring="Python Programming"
# mystring[0]="j" #Not allowed (immutable)
print("Strings are immutable in Python")
print(mystring)

# "Python" → "Jython"  (not possible)